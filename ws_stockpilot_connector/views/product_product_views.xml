<odoo>
    <record id="product_product_tree_view" model="ir.ui.view">
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="after">
                <page string="Stockpilot" name="stockpilot">
                    <field name="stockpilot_ids">
                        <list>
                            <field name="stockpilot_configuration_id" />
                        </list>
                        <form>
                            <field name="stockpilot_configuration_id" />
                            <field name="product_product_id" />
                        </form>
                    </field>
                </page>
            </xpath>
        </field>
    </record>

    <record id="action_export_to_stockpilot" model="ir.actions.server">
        <field name="name">Export to stockpilot</field>
        <field name="model_id" ref="product.model_product_product" />
        <field name="binding_model_id" ref="product.model_product_product" />
        <field name="binding_view_types">list,form</field>
        <field name="state">code</field>
        <field name="code">
records._push_stockpilot_product()
        </field>
    </record>
</odoo>
